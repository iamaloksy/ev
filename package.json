{
  "name": "ev-root-runner",
  "private": true,
  "scripts": {
    "dev": "npm --prefix EV run dev",
    "server": "npm --prefix EV run server",
    "client": "npm --prefix EV run client",
    "build": "npm --prefix EV run build",
    "migrate:old-records": "npm --prefix EV run migrate:old-records"
  }
}
